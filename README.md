# hex-bareiss

The executable fraction-free Bareiss determinant over `Int`, with bordered-minor
support, over the `hex-determinant` / `hex-matrix` cores. Mathlib-free.

Published from the `hex-dev` monorepo; do not edit by hand. See
`SPEC/hex-bareiss.md`.
